{
  "name": "virtual-library",
  "version": "1.0.0",
  "description": "A virtual library app to browse books",
  "private": true,
  "dependencies": {
    "@sentry/react": "^10.34.0",
    "@sentry/webpack-plugin": "^4.8.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:sentry": "npm run build && node scripts/sentry-upload.js",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@sentry/cli": "^3.1.0"
  }
}
